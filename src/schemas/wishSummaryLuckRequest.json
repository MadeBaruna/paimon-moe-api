{
  "type": "object",
  "properties": {
    "banner": {
      "type": "string",
      "enum": ["beginners", "standard", "character-event", "weapon-event"]
    },
    "rarity": {
      "type": "string",
      "enum": ["legendary", "rare"]
    }
  },
  "additionalProperties": false,
  "required": ["banner", "rarity"]
}
